import { defineConfig } from 'vite';
import ViteComponents from 'vite-plugin-components';

export default defineConfig({
  plugins: [
    ViteComponents({
      // configure ViteComponents options here
    })
  ]
});
